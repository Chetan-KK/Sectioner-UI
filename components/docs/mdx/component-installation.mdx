### Installation

{props.folderName}
{props.fileName}

- **Create a Folder**: Make a folder named {props.folderName} inside sectioner/ui folder.
- **Create Component**: Inside {props.folderName}, create a file called {props.fileName}.jsx.
- **Add JSX**: Copy the provided JSX code into {props.fileName}.jsx.
- **Create CSS File**: In the same folder, create a file named {props.fileName}.module.css.
- **Add CSS**: Copy the provided CSS code into {props.fileName}.module.css.
- **Use Component**: Import and use {props.fileName} in your project where needed.