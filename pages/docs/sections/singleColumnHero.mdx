# Single Column hero

import {
  Button,
  Heading,
  SingleColumnHero,
  SubHeading,
} from "@/ui/singleColumnHero/SingleColumnHero";
import ComponentWrapper from "@/components/docs/component-wrapper";

## Overview 

- Static single column hero section
- Fully Responsive single column hero section

### preview

<Tabs items={['Preview','Use']}>
  <Tabs.Tab>
  <ComponentWrapper>
    <SingleColumnHero>
        <Heading>Sectioner UI</Heading>
        <SubHeading>
          Welcome to Sectioner UI, a modern UI library designed to make your web
          designing faster! this UI library contains jsx and module.css code
          sections!
        </SubHeading>
        <Button to={"/explore"}>Explore library</Button>
      </SingleColumnHero>
  </ComponentWrapper>
  </Tabs.Tab>

  <Tabs.Tab>
  
```jsx showLineNumbers filename="App.jsx" copy {2-7,12-20}
import React from "react";
import {
  Button,
  Heading,
  SingleColumnHero,
  SubHeading,
} from "@sectioner/SingleColumnHero/SingleColumnHero";

const App = () => {
  return (
    <div>
      <SingleColumnHero>
        <Heading>Sectioner UI</Heading>
        <SubHeading>
          Welcome to Sectioner UI, a modern UI library designed to make your web
          designing faster! this UI library contains jsx and module.css code
          sections!
        </SubHeading>
        <Button to={"/explore"}>Explore library</Button>
      </SingleColumnHero>
    </div>
  );
};

export default App;
```
  </Tabs.Tab>
  </Tabs>


### installation



- **Create a Folder**: Make a folder named `SingleColumnHero` inside `sectioner` folder.
- **Create Component**: Inside `SingleColumnHero`, create a file called `SingleColumnHero.jsx`.
- **Add JSX**: Copy the provided JSX code into `SingleColumnHero.jsx`.
- **Create CSS File**: In the same folder, create a file named `styles.module.css`.
- **Add CSS**: Copy the provided CSS code into `styles.module.css`.
- **Use Component**: Import and use `SingleColumnHero` in your project where needed.

import { Callout } from 'nextra/components';
 
<Callout type="warning" emoji="⚠">
**note**: Requires [react-router-dom ↗](https://reactrouter.com/en/main/start/tutorial) for redirecting to other page.
</Callout>

import { Tabs } from "nextra/components";

<Tabs items={['jsx', 'css']}>
  
  <Tabs.Tab>

```jsx showLineNumbers filename="sectioner/singleColumnHero/SingleColumnHero.jsx" copy
import { Link } from "react-router-dom";
import styles from "./styles.module.css";

// Main Component Wrapper
export const SingleColumnHero = ({ children, className, ...props }) => {
  return (
    <div className={`${styles.hero} ${className}`} {...props}>
      {children}
    </div>
  );
};

// Heading of component
export const Heading = ({ children, className, ...props }) => {
  return (
    <div className={`${styles.heading} ${className}`} {...props}>
      {children}
    </div>
  );
};

// Sub-Heading of component
export const SubHeading = ({ children, className, ...props }) => {
  return (
    <div className={`${styles.subHeading} ${className}`} {...props}>
      {children}
    </div>
  );
};

// Routing Button
export const Button = ({ children, to, className, ...props }) => {
  return (
    <Link className={`${styles.button} ${className}`} to={to} {...props}>
      {children}
    </Link>
  );
};
```

  </Tabs.Tab>
<Tabs.Tab>
```css showLineNumbers filename="sectioner/singleColumnHero/styles.module.css" copy
.hero {
  padding: 10rem 2rem;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  gap: 1rem;
}

.heading {
  background: -webkit-linear-gradient(
    250deg,
    hsl(var(--secondary)),
    hsl(var(--secondary-forground))
  );
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  font-size: 6rem;
  text-align: center;
  font-weight: 700;
  font-family: var(--ff-poppins);
}

.subHeading {
  width: 45rem;
  color: hsl(var(--sub-text));
  text-align: center;
  font-family: var(--ff-poppins);
}

.button {
  padding: 0.6rem 2.5rem;
  color: hsl(var(--text-forground));
  white-space: nowrap;
  text-decoration: none;
  margin-top: 0.8rem;
  font-weight: 600;
  cursor: pointer;
  border-radius: var(--radius);
  background-image: linear-gradient(
    -30deg,
    hsl(var(--primary)),
    hsl(var(--primary-forground))
  );
  transform: translateY(0);
  transition: box-shadow 250ms, transform 250ms;
  font-family: var(--ff-poppins);
}
.button:hover {
  transform: translateY(-2px);
  box-shadow: 0 2px 20px -5px hsl(var(--primary));
}

.button:focus {
  outline: 2px solid hsl(var(--primary));
  outline-offset: 3px;
}

@media screen and (max-width: 750px) {
  .heading {
    font-size: 4rem;
  }
  .subHeading {
    width: 80vw;
    font-size: 0.9rem;
  }
  .button {
    padding: 0.5rem 2rem;
  }
}

@media screen and (max-width: 450px) {
  .hero {
    align-items: flex-start;
    padding: 10rem 1rem;
  }
  .heading {
    font-size: 3rem;
    line-height: 3.5rem;
    text-align: left;
  }
  .subHeading {
    font-size: 0.8rem;
    text-align: left;
  }
  .button {
    margin-top: 0.5rem;
    font-size: 0.8rem;
  }
}
```
  </Tabs.Tab>
</Tabs>

#### Props

Use the following props to customize the component.

{<table className="nx-mt-6 nx-p-0 first:nx-mt-0">
  <thead>
    <tr>
      <th align="center">Prop</th>
      <th align="center">Description</th>
    </tr>
  </thead>
  <tbody>
  <tr>
      <td>heading</td>
      <td>Main heading of section</td>
    </tr>
    <tr>
      <td>subHeading</td>
      <td>Sub-heading of section</td>
    </tr>
    <tr>
      <td>buttonUrl</td>
      <td>Router URL where button will route {"("} Uses <a target="_blank" href="https://reactrouter.com/en/main/components/link">Link ↗</a> from <a target="_blank" href="https://reactrouter.com/en/main/start/tutorial">React-router-dom ↗</a> {")"}</td>
    </tr>
    <tr>
      <td>buttonText</td>
      <td>Text inside button</td>
    </tr>
  </tbody>
</table>
}

#### Folder Structure

import { FileTree } from 'nextra/components'
 
<FileTree>
  <FileTree.Folder name="your-project" defaultOpen>
  <FileTree.Folder name="src" defaultOpen>
    <FileTree.Folder name="components" defaultOpen>
    <FileTree.Folder name="sectioner" defaultOpen>
      <FileTree.Folder name="singleColumnHero" defaultOpen>
        
    <FileTree.File name="SingleColumnHero.jsx" />
    <FileTree.File name="styles.module.css" />
    </FileTree.Folder>  
    </FileTree.Folder>
    </FileTree.Folder>
    
    
  </FileTree.Folder>
  </FileTree.Folder>
</FileTree>