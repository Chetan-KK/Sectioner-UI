# Single Column hero

import {
  Heading,
  SingleColumnHero,
  SubHeading,
  ButtonsWrapper
} from "@/sectioner/singleColumnHero/SingleColumnHero";
import {
  Button, LinkButton
} from "@/sectioner/ui/Button/Button";

import SectionWrapper from "@/components/docs/section-wrapper";

## Overview 

- Static single column hero section
- Fully Responsive single column hero section

### preview

import { Tabs } from "nextra/components";


<Tabs items={['Preview','Use']}>
  <Tabs.Tab>
  <SectionWrapper>
      <SingleColumnHero>
        <Heading>Sectioner UI</Heading>
        <SubHeading>
          Welcome to Sectioner UI, a modern UI library designed to make your web
          designing faster! this UI library contains jsx and module.css code
          sections!
        </SubHeading>
        <ButtonsWrapper>
          <Button to={"/explore"} varient={"outline"}>Explore Components</Button>
          <Button to={"/explore"}>Explore Sections</Button>
        </ButtonsWrapper>
      </SingleColumnHero>
  </SectionWrapper>
  </Tabs.Tab>

  <Tabs.Tab>
  
```jsx showLineNumbers filename="App.jsx" copy {2-8,13-24}
import React from "react";
import {
  Heading,
  SingleColumnHero,
  SubHeading,
  ButtonsWrapper
} from "components/sectioner/singleColumnHero/SingleColumnHero";
import { Button, LinkButton } from "components/sectioner/ui/Button/Button";

const Hero = () => {
  return (
    <div>
      <SingleColumnHero>
        <Heading>Sectioner UI</Heading>
        <SubHeading>
          Welcome to Sectioner UI, a modern UI library designed to make your web
          designing faster! this UI library contains jsx and module.css code
          sections!
        </SubHeading>
        <ButtonsWrapper>
          <LinkButton to={"/explore"} varient={"outline"}>Explore Components</LinkButton>
          <LinkButton to={"/explore"}>Explore Sections</LinkButton>
        </ButtonsWrapper>
      </SingleColumnHero>
    </div>
  );
};

export default Hero;
```
  </Tabs.Tab>
  </Tabs>


### installation

- **Create a Folder**: Make a folder named `SingleColumnHero` inside `sectioner` folder.
- **Create Component**: Inside `singleColumnHero`, create a file called `SingleColumnHero.jsx`.
- **Add JSX**: Copy the provided JSX code into `SingleColumnHero.jsx`.
- **Create CSS File**: In the same folder, create a file named `styles.module.css`.
- **Add CSS**: Copy the provided CSS code into `styles.module.css`.
- **Use Component**: Import and use `SingleColumnHero` in your project where needed.


<Tabs items={['jsx', 'css']}>
  
  <Tabs.Tab>

```jsx showLineNumbers filename="sectioner/singleColumnHero/SingleColumnHero.jsx" copy
import styles from "./styles.module.css";

// Main Component Wrapper
export const SingleColumnHero = ({ children, className, ...props }) => {
  return (
    <div className={`${styles.hero} ${className}`} {...props}>
      {children}
    </div>
  );
};

// Heading of component
export const Heading = ({ children, className, ...props }) => {
  return (
    <div className={`${styles.heading} ${className}`} {...props}>
      {children}
    </div>
  );
};

// Sub-Heading of component
export const SubHeading = ({ children, className, ...props }) => {
  return (
    <div className={`${styles.subHeading} ${className}`} {...props}>
      {children}
    </div>
  );
};

// ButtonsWrapper
export const ButtonsWrapper = ({ children, className, ...props }) => {
  return (
    <div className={`${styles.buttonsWrapper} ${className}`} {...props}>
      {children}
    </div>
  );
};
```

  </Tabs.Tab>
<Tabs.Tab>
```css showLineNumbers filename="sectioner/singleColumnHero/styles.module.css" copy
.hero {
  padding: 10rem 2rem;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  gap: 1rem;
}

.heading {
  background: -webkit-linear-gradient(
    250deg,
    hsl(var(--primary)),
    hsl(var(--secondary))
  );
  line-height: 120%;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  font-size: 5rem;
  text-align: center;
  font-weight: 700;
  font-family: var(--ff-poppins);
}

.subHeading {
  width: 45rem;
  color: hsl(var(--muted));
  text-align: center;
  font-family: var(--ff-poppins);
  margin-bottom: 0.4rem;
}

.buttonsWrapper {
  display: flex;
  gap: 0.7rem;
  align-items: center;
  justify-content: center;
  flex-wrap: wrap;
}

@media screen and (max-width: 750px) {
  .heading {
    font-size: 3.5rem;
  }
  .subHeading {
    width: 80vw;
    font-size: 0.9rem;
  }
  .button {
    padding: 0.5rem 2rem;
  }
}

@media screen and (max-width: 450px) {
  .hero {
    align-items: flex-start;
    padding: 10rem 1rem;
  }
  .heading {
    font-size: 2.7rem;
    line-height: 3.5rem;
    text-align: left;
  }
  .subHeading {
    width: 90vw;
    font-size: 0.8rem;
    text-align: left;
  }
  .button {
    margin-top: 0.5rem;
    font-size: 0.8rem;
  }

  .buttonsWrapper {
    justify-content: flex-start;
  }
}
```
  </Tabs.Tab>
</Tabs>

#### Components

Use the following props to customize the component.

{<table className="nx-mt-6 nx-p-0 first:nx-mt-0">
  <thead>
    <tr>
      <th align="center">Component</th>
      <th align="center">Description</th>
    </tr>
  </thead>
  <tbody>
  <tr>
      <td>SingleColumnHero</td>
      <td>Section</td>
    </tr>
  <tr>
      <td>Heading</td>
      <td>Main heading of section</td>
    </tr>
    <tr>
      <td>SubHeading</td>
      <td>Sub-heading of section</td>
    </tr>
    <tr>
      <td>ButtonsWrapper</td>
      <td>Wrapper for buttons</td>
    </tr>
    <tr>
      <td>Button</td>
      <td> <a href="/docs/components/button">Button</a> Component</td>
    </tr>
  </tbody>
</table>
}

#### Folder Structure

import { FileTree } from 'nextra/components'
 
<FileTree>
  <FileTree.Folder name="your-project" defaultOpen>
  <FileTree.Folder name="src" defaultOpen>
    <FileTree.Folder name="components" defaultOpen>
    <FileTree.Folder name="sectioner" defaultOpen>
      <FileTree.Folder name="singleColumnHero" defaultOpen>
        
    <FileTree.File name="SingleColumnHero.jsx" />
    <FileTree.File name="styles.module.css" />
    </FileTree.Folder>  
    </FileTree.Folder>
    </FileTree.Folder>
    
    
  </FileTree.Folder>
  </FileTree.Folder>
</FileTree>