# Button

## Overview 

- Button is required for many sections.


import { Callout } from 'nextra/components';
 
<Callout type="warning" emoji="⚠">
**note**: Requires [react-router-dom ↗](https://reactrouter.com/en/main/start/tutorial) for redirecting to other page.
</Callout>

### preview

import { Tabs } from "nextra/components";
import ComponentWrapper from "@/components/docs/component-wrapper";
import {
    Button, LinkButton
} from "@/sectioner/ui/Button/Button";
import { LayoutTemplate } from 'lucide-react'

<Tabs items={['Preview','Use']}>
<Tabs.Tab>
<ComponentWrapper classname="gap-3 flex-wrap">
    <Button>Default</Button>
    <Button varient="outline">Outline</Button>
    <Button varient="ghost">Ghost</Button>
    <LinkButton to={"/dashboard"}>Link Button</LinkButton>
    <LinkButton to={"/dashboard"} size="icon" varient="outline"><LayoutTemplate className='h-5 w-5'/></LinkButton>
    <Button varient="danger">Danger</Button>
    <Button varient="success">Success</Button>
</ComponentWrapper>
</Tabs.Tab>
<Tabs.Tab>
```jsx showLineNumbers filename="App.jsx" copy
<Button>Button</Button>
<Button varient="outline">Button</Button>
<Button varient="ghost">Button</Button>
<LinkButton to={"/dashboard"}>Dashboard</LinkButton>
<Button varient="danger">Danger</Button>
<Button varient="success">Success</Button>
```
</Tabs.Tab>
</Tabs>

### Installation

- **Create a Folder**: Make a folder named `Button` inside `sectioner/ui` folder.
- **Create Component**: Inside `Button`, create a file called `Button.jsx`.
- **Add JSX**: Copy the provided JSX code into `Button.jsx`.
- **Create CSS File**: In the same folder, create a file named `styles.module.css`.
- **Add CSS**: Copy the provided CSS code into `styles.module.css`.
- **Use Component**: Import and use `Button` in your project where needed.

<Tabs items={['jsx','css']}>
<Tabs.Tab>
```jsx showLineNumbers filename="Button.jsx" copy
import React from "react";
import styles from "./styles.module.css";
import { Link } from "react-router-dom";

export const Button = ({
  children,
  classname,
  varient = "default",
  size = "default",
  ...props
}) => {
  const stylesVarientMap = {
    default: styles.default,
    outline: styles.outline,
    ghost: styles.ghost,
    danger: styles.danger,
    success: styles.success,
  };
  const stylesSizeMap = {
    default: styles.defaultSize,
    icon: styles.iconSize,
  };

  return (
    <button
      className={`${styles.button} ${stylesSizeMap[size]} ${stylesVarientMap[varient]} ${classname}`}
      {...props}
    >
      {children}
    </button>
  );
};

export const LinkButton = ({
  children,
  classname,
  varient = "default",
  size = "default",
  to,
  ...props
}) => {
  const stylesVarientMap = {
    default: styles.default,
    outline: styles.outline,
    ghost: styles.ghost,
    danger: styles.danger,
    success: styles.success,
  };
  const stylesSizeMap = {
    default: styles.defaultSize,
    icon: styles.iconSize,
  };

  return (
    <Link
      to={to}
      className={`${styles.button} ${stylesSizeMap[size]} ${stylesVarientMap[varient]} ${classname}`}
      {...props}
    >
      {children}
    </Link>
  );
};
```
</Tabs.Tab>
<Tabs.Tab>
```css showLineNumbers filename="styles.module.css" copy
.button {
  white-space: nowrap;
  padding: 0.5rem 1.1rem;
  height: 100%;
  width: max-content;
  display: flex;
  align-items: center;
  gap: 0.7em;
  border-radius: var(--radius);
  cursor: pointer;
  transition: background-color 250ms;
  text-decoration: none;
  font-size: inherit;
  font-family: var(--ff-poppins);
}
/* default varient */
.default {
  background-color: hsl(var(--secondary));
  border: none;
  color: hsl(var(--text));
}
.default:hover {
  background-color: hsl(var(--secondary), 0.7);
}

/* outline varient */
.outline {
  border: 1px solid hsl(var(--border));
  background-color: transparent;
  color: hsl(var(--text));
}

.outline:hover {
  background-color: hsl(var(--border), 0.2);
}

/* ghost varient */
.ghost {
  border: none;
  background-color: transparent;
  color: hsl(var(--text));
}

.ghost:hover {
  background-color: hsl(var(--border), 0.3);
}

/* danger varient */
.danger {
  background-color: hsl(var(--danger));
  border: none;
  color: hsl(var(--text-forground));
}

.danger:hover {
  background-color: hsl(var(--danger), 0.7);
}

/* success varient */
.success {
  background-color: hsl(var(--success));
  border: none;
  color: hsl(var(--text-forground));
}

.success:hover {
  background-color: hsl(var(--success), 0.7);
}

/* iconSize */
.iconSize {
  aspect-ratio: 1;
  width: 40px;
  height: 100%;
  padding: 0;
  display: flex;
  justify-content: center;
  align-items: center;
}

@media screen and (max-width: 750px) {
  .button {
    font-size: 0.8rem;
  }
  .iconSize {
    width: 35px;
  }
}
```
</Tabs.Tab>
</Tabs>
