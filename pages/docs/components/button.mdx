# Button

## Overview 

- Button is required for many sections.


import { Callout } from 'nextra/components';
 
<Callout type="info" emoji="!!">
**note**: Users [react-router-dom ↗](https://reactrouter.com/en/main/start/tutorial) for redirecting to other page.
</Callout>

### preview

import { Tabs } from "nextra/components";
import ComponentWrapper from "@/components/docs/component-wrapper";
import {
  Button
} from "@/sectioner/ui/Button/Button";
import { LayoutTemplate } from 'lucide-react';

#### Normal Buttons

<Tabs items={['Preview','Use']}>
<Tabs.Tab>
<ComponentWrapper classname="gap-3 flex-wrap">
    <Button>Default</Button>
    <Button variant="outline">Outline</Button>
    <Button variant="ghost">Ghost</Button>
    <Button to={"/dashboard"}>Link Button</Button>
    <Button to={"/dashboard"} size="icon" variant="outline"><LayoutTemplate className='h-5 w-5'/></Button>
    <Button variant="danger">Danger</Button>
    <Button variant="success">Success</Button>
</ComponentWrapper>
</Tabs.Tab>
<Tabs.Tab>
```jsx showLineNumbers filename="App.jsx" copy
<Button>Default</Button>
<Button variant="outline">Outline</Button>
<Button variant="ghost">Ghost</Button>
<Button to={"/dashboard"}>Link Button</Button>
<Button to={"/dashboard"} size="icon" variant="outline"><LayoutTemplate className='h-5 w-5'/></Button>
<Button variant="danger">Danger</Button>
<Button variant="success">Success</Button>
```
</Tabs.Tab>
</Tabs>

#### Disabled Buttons

- Use `disabled` to disable Buttons.

<Tabs items={['Preview','Use']}>
<Tabs.Tab>
<ComponentWrapper classname="gap-3 flex-wrap">
    <Button disabled>Default</Button>
    <Button disabled variant="outline">Outline</Button>
    <Button disabled variant="ghost">Ghost</Button>
    <Button disabled to={"/dashboard"}>Link Button</Button>
    <Button disabled to={"/dashboard"} size="icon" variant="outline"><LayoutTemplate className='h-5 w-5'/></Button>
    <Button disabled variant="danger">Danger</Button>
    <Button disabled variant="success">Success</Button>
</ComponentWrapper>
</Tabs.Tab>
<Tabs.Tab>
```jsx showLineNumbers filename="App.jsx" copy
<Button disabled>Default</Button>
<Button disabled variant="outline">Outline</Button>
<Button disabled variant="ghost">Ghost</Button>
<Button disabled to={"/dashboard"}>Link Button</Button>
<Button disabled to={"/dashboard"} size="icon" variant="outline"><LayoutTemplate className='h-5 w-5'/></Button>
<Button disabled variant="danger">Danger</Button>
<Button disabled variant="success">Success</Button>
```
</Tabs.Tab>
</Tabs>


{/* mdx import */}
import ComponentInstallation from '@/components/docs/mdx/component-installation.mdx';

<ComponentInstallation fileName="Button" folderName="button" />


<Tabs items={['jsx','css']}>
<Tabs.Tab>
```jsx showLineNumbers filename="Button.jsx" copy
import React from "react";
import styles from "./Button.module.css";
import { Link } from "react-router-dom";

export const Button = ({
  children,
  classname,
  variant = "default",
  size = "default",
  disabled = false,
  to,
  ...props
}) => {
  const stylesVariantMap = {
    default: styles.default,
    outline: styles.outline,
    ghost: styles.ghost,
    danger: styles.danger,
    success: styles.success,
  };
  const stylesSizeMap = {
    default: styles.defaultSize,
    icon: styles.iconSize,
  };

  const classNames = `${styles.button} ${stylesSizeMap[size]} ${
    stylesVariantMap[variant]
  } ${classname} ${disabled && "disabled"}`;

  if (to) {
    return (
      <Link
        to={to}
        tabIndex={disabled ? "-1" : "false"}
        className={classNames}
        {...props}
      >
        {children}
      </Link>
    );
  }

  return (
    <button disabled={disabled} className={classNames} {...props}>
      {children}
    </button>
  );
};
```
</Tabs.Tab>
<Tabs.Tab>
```css showLineNumbers filename="Button.module.css" copy
.button {
  white-space: nowrap;
  padding: 0.5rem 1.1rem;
  height: 100%;
  width: max-content;
  display: flex;
  align-items: center;
  gap: 0.7em;
  border-radius: var(--radius-full);
  cursor: pointer;
  transition: background-color 250ms;
  text-decoration: none;
  font-size: 16px;
  font-family: var(--ff-poppins);
  overflow: hidden;
}
/* default variant */
.default {
  background-color: hsl(var(--secondary));
  border: none;
  color: hsl(var(--white));
}
.default:hover {
  background-color: hsl(var(--secondary), 0.8);
}

/* outline variant */
.outline {
  border: 1px solid hsl(var(--border));
  background-color: transparent;
  color: hsl(var(--text));
}

.outline:hover {
  background-color: hsl(var(--border), 0.2);
}

/* ghost variant */
.ghost {
  border: none;
  background-color: transparent;
  color: hsl(var(--text));
}

.ghost:hover {
  background-color: hsl(var(--border), 0.3);
}

.ghost:focus {
  box-shadow: 0 0 1px 3px hsl(var(--text), 0.3);
}

/* danger variant */
.danger {
  background-color: hsl(var(--danger));
  border: none;
  color: hsl(var(--white));
}

.danger:hover {
  background-color: hsl(var(--danger), 0.8);
}

.danger:focus {
  box-shadow: 0 0 1px 3px hsl(var(--danger), 0.5);
}

/* success variant */
.success {
  background-color: hsl(var(--success));
  border: none;
  color: hsl(var(--white));
}

.success:hover {
  background-color: hsl(var(--success), 0.8);
}

.success:focus {
  box-shadow: 0 0 1px 3px hsl(var(--success), 0.5);
}

/* iconSize */
.iconSize {
  aspect-ratio: 1;
  width: 40px;
  height: 100%;
  padding: 0;
  display: flex;
  justify-content: center;
  align-items: center;
}

.button:disabled,
.button[disabled] {
  opacity: 0.5;
}

.button:disabled,
.button[disabled] {
  pointer-events: none;
  opacity: 0.6; /* Makes it look faded */
}

@media screen and (max-width: 750px) {
  .button {
    font-size: 0.8rem;
  }
  .iconSize {
    width: 35px;
  }
}
```
</Tabs.Tab>
</Tabs>

### props

- variants: `default`,`outline`,`ghost`,`danger`,`success`

- sizes: `default`,`icon`