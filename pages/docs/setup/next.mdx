# Next JS Setup
### steps

import { Tabs } from "nextra/components";
import { Steps } from 'nextra/components'
 
{/* 
unfinished setup
 */}

 
<Steps>
### Create Project

Start by creating a new Next.js project using `create-next-app`:

<Tabs items={["npm", "yarn", "pnpm", "bun"]}>
  <Tabs.Tab>
  ```bash copy
  npx create-next-app@latest my-app --typescript
  ```
  </Tabs.Tab>
  <Tabs.Tab>
  ```bash copy
  yarn create next-app@latest my-app --typescript
  ```
  </Tabs.Tab>
  <Tabs.Tab>
  ```bash copy
  pnpm create next-app@latest my-app --typescript
  ```
  </Tabs.Tab>
  <Tabs.Tab>
  ```bash copy
  bunx --bun create-next-app@latest my-app --typescript
  ```
  </Tabs.Tab>
</Tabs>
 
### Setup css

Copy and replace the following CSS code into your `globals.css` file

```css copy
/* importing poppins font */
@import url("https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap");

/* removing extra margin and padding */
*,
*::before,
*::after {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

/* css variables used in components */
:root {
  --primary: 200 100% 50%;
  --primary-forground: 218 100% 50%;
  --secondary: 319 100% 50%;
  --secondary-forground: 277 100% 50%;
  --text: 255 100% 0%;
  --text-forground: 255 100% 100%;
  --sub-text: 255 0% 30%;
  font-family: "Poppins", Tahoma, Geneva, Verdana, sans-serif;
}

/* css variables for dark mode */
.dark {
  --primary: 200 100% 50%;
  --primary-forground: 218 100% 50%;
  --secondary: 319 100% 50%;
  --secondary-forground: 277 100% 50%;
  --text: 255 100% 100%;
  --text-forground: 255 100% 0%;
  --sub-text: 255 0% 30%;
}
```
This code defines the global color variables and imports the 'Poppins' font for consistent styling across your application.

### Folder Structure

Create a folder named `sectioner` within the `src/components` directory. Organize your components into separate folders within `sectioner` for better project structure and maintainability.

import { Callout } from 'nextra/components'
 
<Callout type="warning" emoji="💡">
**note**: It's recommended to create a separate folder for each section or component within the `sectioner` directory.
</Callout>




import { FileTree } from 'nextra/components'
 
<FileTree>
  <FileTree.Folder name="your-project" defaultOpen>
  <FileTree.Folder name="src" defaultOpen>
    <FileTree.Folder name="components" defaultOpen>
    <FileTree.Folder name="sectioner" defaultOpen>
      <FileTree.Folder name="component-name" defaultOpen>
        
    <FileTree.File name="sectioner-component.jsx" />
    <FileTree.File name="sectioner-component.css" />
    </FileTree.Folder>  
    </FileTree.Folder>
    </FileTree.Folder>
    
    
  </FileTree.Folder>
  </FileTree.Folder>
</FileTree>

</Steps>
