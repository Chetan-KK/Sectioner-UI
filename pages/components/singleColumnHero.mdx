# Single Column hero

import SingleColumnHero from "@/components/SingleColumnHero/SingleColumnHero";

## Overview

Fully Responsive single column hero section

## preview

<SingleColumnHero
  heading="Chetan Khulage"
  subHeading="I love to work on front-end of web and i mostly work with THREE js for 3D web development, i have worked on web development from around 3 years"
  buttonUrl="/explore"
  buttonText="explore my Projects"
/>

## code

```jsx copy
import styles from "./styles.module.css";

const SingleColumnHero = ({
  heading,
  subHeading,
  buttonUrl,
  buttonText,
  className,
}) => {
  return (
    <div className={`${styles.hero} ${className}`}>
      <h1 className={styles.heading}>{heading}</h1>
      <p className={styles.subHeading}>{subHeading}</p>
      <button className={styles.button}>{buttonText}</button>
    </div>
  );
};

export default SingleColumnHero;
```
